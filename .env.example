# Commissioner sign-in (placeholder until email/OAuth sign-in exists).
COMMISSIONER_PASSCODE=
# 32+ random characters, e.g. openssl rand -hex 32
SESSION_SECRET=
# Production database (Turso). Omit locally: data/league.db is used.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
